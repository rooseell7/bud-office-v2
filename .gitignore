# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment (secrets)
.env
!.env.example

# Uploads (user files)
Backend/uploads/

# Nginx (local install, binaries)
nginx/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ZIP-бекапи проєкту (створюються make-backup.ps1)
BUD_office_backup_*.zip

# Локальна папка бекапів (куди make-backup.ps1 зберігає архіви)
_backups/

# Помилкові файли (наприклад, випадково створені)
Backend/\{

# Cursor: ignore all local cursor stuff by default
.cursor/**

# Cursor: track rules/commands as source of truth
!.cursor/rules/
!.cursor/rules/**
!.cursor/commands/
!.cursor/commands/**
# NOTE: do NOT allowlist settings.json (keep it local/untracked)
