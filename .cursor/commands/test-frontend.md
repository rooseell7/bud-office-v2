Run frontend verification using package.json scripts.

1) cd to frontend folder.
2) Run typecheck (if script exists).
3) Run build.
4) Run frontend tests (unit/e2e) if configured.
Return results and failing command logs summary.
